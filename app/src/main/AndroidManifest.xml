<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/pato_pocho"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/pato_pocho"
        android:supportsRtl="true"
        android:theme="@style/Theme.Comanderlifecounter"
        tools:targetApi="31">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".model.Pop"
            android:theme="@style/CustomTheme"></activity>

        <activity android:name=".model.Settings"
            android:theme="@style/CustomTheme"></activity>

        <activity
            android:name="com.example.comander_lifecounter.GameLogic"
            android:exported="false"
            android:screenOrientation="portrait">
        </activity>

    </application>

    <uses-permission android:name="android.permission.INTERNET"/>

</manifest>
